name: Deploy to BM_SB on Merge

on:
  push:
    branches:
      - deployment-changes-second-step

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout Repository
        uses: actions/checkout@v3

      - name: 💾 Install Salesforce CLI
        run: npm install -g sfdx-cli

      - name: 🔐 Decode Auth URL
        run: echo "${{ secrets.BM_SB_AUTH_URL }}" > auth_url.txt



      - name: 🚀 Deploy to BM_SB Sandbox
        run: sfdx force:source:deploy -u BM_SB -p force-app --json --loglevel fatal
